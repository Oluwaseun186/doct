apiVersion: apps/v1
kind: Deployment
metadata:
  name: Deployment
  namespace: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
        - name: "Docker image"
          image: oluwaseun7/dmailer:1.0.1
          imagePullPolicy: Always
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"          
          ports:
            - containerPort: 3001
