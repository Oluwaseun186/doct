apiVersion: v1
kind: Service
metadata:
  name: service
spec:
  type: LoadBalancer
  selector:
    app: testing111
  ports:
    - port: 80
      targetPort: 3000



# ---      

# apiVersion: v1
# kind: Service
# metadata:
#   name: service2
# spec:
#   type: LoadBalancer
#   selector:
#     app: testing111
#   ports:
#     - port: 3001
#       targetPort: 3002